using UnityEngine;

public class PrintNama : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Nama Saya Adalah : Ahay");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
